import { r as registerInstance, h, g as getElement } from './index-10623738.js';

const audioCss = "gx-audio audio.disabled{pointer-events:none}";

const Audio = class {
    constructor(hostRef) {
        registerInstance(this, hostRef);
        /**
         * This attribute lets you specify if the element is disabled.
         */
        this.disabled = false;
    }
    render() {
        return (h("audio", { class: { disabled: this.disabled }, controls: true, src: this.src }));
    }
    get element() { return getElement(this); }
};
Audio.style = audioCss;

export { Audio as gx_audio };
