import { h } from "@stencil/core";
export class ProgressBarRender {
    constructor(component) {
        this.component = component;
    }
    render(slots) {
        return [
            h("gx-bootstrap", null),
            h("div", { class: "progress" },
                h("div", { class: "progress-bar progress-bar-striped progress-bar-animated bg-primary", style: { width: this.component.value + "%" }, "aria-valuenow": this.component.value, "aria-valuemin": "0", "aria-valuemax": "100" }, slots.default))
        ];
    }
}
