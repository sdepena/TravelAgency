import { EventEmitter } from "../../stencil-public-runtime";
import { Component as GxComponent } from "../common/interfaces";
export declare class MapLine implements GxComponent {
    element: HTMLGxMapLineElement;
    private lineInstance;
    /**
     * The coordinates where the line/polyline will appear in the map.
     *
     */
    coords: string;
    /**
     * Emmits when the element is added to a `<gx-map>`.
     *
     */
    gxMapLineDidLoad: EventEmitter;
    /**
     * Emmits when the element is deleted from a `<gx-map>`.
     *
     */
    gxMapLineDeleted: EventEmitter;
    private setupLine;
    componentDidLoad(): void;
    componentDidUnload(): void;
    render(): any;
}
