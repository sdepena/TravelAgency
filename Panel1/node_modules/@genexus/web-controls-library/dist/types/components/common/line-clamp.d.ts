import { Component } from "./interfaces";
export declare function makeLinesClampable(component: LineClampComponent, contentElementSelect: string, lineMeasuringElementSelector: string): {
    applyLineClamp: () => void;
    resetLineClamp: () => void;
};
export interface LineClampComponent extends Component {
    lineClamp: boolean;
    maxLines: number;
    maxHeight: number;
}
