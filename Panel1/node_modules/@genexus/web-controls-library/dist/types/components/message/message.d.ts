import { Component as GxComponent, VisibilityComponent } from "../common/interfaces";
export declare class Message implements GxComponent, VisibilityComponent {
    constructor();
    private renderer;
    element: HTMLGxMessageElement;
    /**
     * This attribute lets you specify how this element will behave when hidden.
     *
     * | Value        | Details                                                                     |
     * | ------------ | --------------------------------------------------------------------------- |
     * | `keep-space` | The element remains in the document flow, and it does occupy space.         |
     * | `collapse`   | The element is removed form the document flow, and it doesn't occupy space. |
     */
    readonly invisibleMode: "collapse" | "keep-space";
    /**
     * Show a button for closing the meesage box
     */
    readonly showCloseButton: boolean;
    /**
     * Text for the close button.
     */
    readonly closeButtonText: string;
    /**
     * Type of the button:
     * * `info`: Information message
     * * `warning`: Warning Message
     * * `error`: Error message
     */
    readonly type: "info" | "warning" | "error";
    /**
     * The time in miliseconds before the message is automatically dismissed.
     * If no duration is specified, the message will not be automatically dismissed.
     */
    readonly duration: number;
    componentDidLoad(): void;
    render(): any;
}
