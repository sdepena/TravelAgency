import { Renderer } from "../../../common/interfaces";
import { FormField } from "../../../form-field/form-field";
export declare class FormFieldRender implements Renderer {
    private component;
    constructor(component: FormField);
    private formFieldId;
    private LABEL_WIDTH_BY_POSITION;
    private INNER_CONTROL_WIDTH_BY_LABEL_POSITION;
    private getLabelCssClass;
    private getInnerControlContainerClass;
    private shouldRenderLabelBefore;
    componentDidLoad(): Promise<void>;
    renderForRadio(renderLabel: boolean, renderLabelBefore: boolean, slot: any): any;
    render(slots: any): any;
}
