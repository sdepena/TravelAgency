import { Renderer } from "../../../common/interfaces";
import { PasswordEdit } from "../../../password-edit/password-edit";
export declare class PasswordEditRender implements Renderer {
    private component;
    constructor(component: PasswordEdit);
    private innerEdit;
    getNativeInputId(): any;
    private getValueFromEvent;
    handleChange(event: CustomEvent<any>): void;
    handleInput(event: CustomEvent<any>): void;
    /**
     * Update the native input element when the value changes
     */
    valueChanged(): void;
    componentDidUnload(): void;
    render(): any;
}
