import { Renderer } from "../../../common/interfaces";
import { ProgressBar } from "../../../progress-bar/progress-bar";
export declare class ProgressBarRender implements Renderer {
    private component;
    constructor(component: ProgressBar);
    render(slots: any): any[];
}
