import { Renderer } from "../../../common/interfaces";
import { Switch } from "../../../switch/switch";
export declare class SwitchRender implements Renderer {
    private component;
    constructor(component: Switch);
    private inputId;
    getNativeInputId(): string;
    private getNativeInput;
    private getValueFromEvent;
    handleChange(event: UIEvent): void;
    /**
     * Update the native input element when the value changes
     */
    checkedChanged(): void;
    render(): any[];
}
