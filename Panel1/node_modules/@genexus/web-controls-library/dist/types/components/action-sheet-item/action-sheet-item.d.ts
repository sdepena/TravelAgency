import { Component as GxComponent } from "../common/interfaces";
export declare class ActionSheetItem implements GxComponent {
    constructor();
    element: HTMLGxActionSheetItemElement;
    /**
     * This attribute lets you specify the type of action. `"cancel"` and `"destructive"` are styled differently
     */
    readonly actionType: "default" | "destructive";
    /**
     * This attribute lets you specify if the element is disabled.
     * If disabled, it will not fire any user interaction related event
     * (for example, gxClick event).
     */
    readonly disabled = false;
    private handleClick;
    render(): any;
}
