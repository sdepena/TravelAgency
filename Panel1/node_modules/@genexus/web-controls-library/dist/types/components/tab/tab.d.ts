import { EventEmitter } from "../../stencil-public-runtime";
import { Component as GxComponent, VisibilityComponent } from "../common/interfaces";
import { HighlightableComponent } from "../common/highlightable";
export declare class Tab implements GxComponent, VisibilityComponent, HighlightableComponent {
    element: HTMLGxTabElement;
    private lastSelectedTab;
    /**
     * This attribute lets you specify how this element will behave when hidden.
     *
     * | Value        | Details                                                                     |
     * | ------------ | --------------------------------------------------------------------------- |
     * | `keep-space` | The element remains in the document flow, and it does occupy space.         |
     * | `collapse`   | The element is removed form the document flow, and it doesn't occupy space. |
     */
    readonly invisibleMode: "collapse" | "keep-space";
    /**
     * True to highlight control when an action is fired.
     */
    readonly highlightable = false;
    /**
     * Fired when the active tab is changed
     *
     */
    tabChange: EventEmitter;
    tabClickHandler(event: CustomEvent): void;
    private setSelectedTab;
    private getCaptionSlots;
    private mapPageToCaptionSelection;
    componentDidLoad(): void;
    componentDidUpdate(): void;
    disconnectedCallback(): void;
    private linkTabs;
    render(): any;
    private setCaptionSlotsClass;
    private setPageSlotsClass;
    private getPageSlots;
}
