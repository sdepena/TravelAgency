import { EventEmitter } from "../../stencil-public-runtime";
import { ClickableComponent, Component as GxComponent, DisableableComponent, VisibilityComponent } from "../common/interfaces";
import { Swipeable } from "../common/swipeable";
export declare class Canvas implements GxComponent, VisibilityComponent, DisableableComponent, Swipeable, ClickableComponent {
    constructor();
    element: HTMLGxCanvasElement;
    /**
     * This attribute lets you specify how this element will behave when hidden.
     *
     * | Value        | Details                                                                     |
     * | ------------ | --------------------------------------------------------------------------- |
     * | `keep-space` | The element remains in the document flow, and it does occupy space.         |
     * | `collapse`   | The element is removed form the document flow, and it doesn't occupy space. |
     */
    readonly invisibleMode: "collapse" | "keep-space";
    /**
     * This attribute lets you specify if the element is disabled.
     * If disabled, it will not fire any user interaction related event
     * (for example, click event).
     */
    readonly disabled = false;
    /**
     * Emitted when the element is clicked.
     */
    gxClick: EventEmitter;
    /**
     * Emitted when the element is swiped.
     */
    swipe: EventEmitter;
    /**
     * Emitted when the element is swiped in upward direction.
     */
    swipeUp: EventEmitter;
    /**
     * Emitted when the element is swiped right direction.
     */
    swipeRight: EventEmitter;
    /**
     * Emitted when the element is swiped downward direction.
     */
    swipeDown: EventEmitter;
    /**
     * Emitted when the element is swiped left direction..
     */
    swipeLeft: EventEmitter;
    width: number;
    height: number;
    private watchForItemsObserver;
    private handleClick;
    componentDidLoad(): void;
    disconnectedCallback(): void;
    private calculateDimensions;
    render(): any;
}
