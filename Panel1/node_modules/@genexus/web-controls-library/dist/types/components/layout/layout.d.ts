import { EventEmitter } from "../../stencil-public-runtime";
import { Component as GxComponent } from "../common/interfaces";
export declare class Layout implements GxComponent {
    constructor();
    element: HTMLGxLayoutElement;
    /**
     * True to hide the top target
     */
    readonly topHidden = false;
    /**
     * True to hide the right target
     */
    rightHidden: boolean;
    /**
     * True to hide the bottom target
     */
    readonly bottomHidden = false;
    /**
     * True to hide the left target
     */
    leftHidden: boolean;
    isMaskVisible: boolean;
    /**
     * Fired when the leftHidden property is changed
     */
    leftHiddenChange: EventEmitter;
    /**
     * Fired when the rightHidden property is changed
     */
    rightHiddenChange: EventEmitter;
    /**
     * Fired when the viewport size is less than the vertical targets breakpoint.
     */
    verticalTargetsBreakpointMatchChange: EventEmitter;
    private mediaQueryList;
    private isVerticalTargetsBreakpoint;
    handleRightHiddenChange(): void;
    handleLeftHiddenChange(): void;
    private updateMaskVisibility;
    private handleBodyClick;
    componentDidLoad(): void;
    disconnectedCallback(): void;
    private startMediaQueryMonitoring;
    private handleMediaQueryChange;
    private updateVerticalTargetsBreakpointStatus;
    private endMediaQueryMonitoring;
    render(): any;
}
